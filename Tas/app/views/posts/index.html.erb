<h1>Social Networks keyword search</h1>

<!-- <table>
  <thead>
    <tr>
      <th>Text</th>
      <th>Frequency</th>
      <th>Author</th>
      <th>Postdate</th>
      <th>Source</th>
      <th>Query</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @posts.each do |post| %>
      <tr>
        <td><%= post.text %></td>
        <td><%= post.frequency %></td>
        <td><%= post.author %></td>
        <td><%= post.postDate %></td>
        <td><%= post.source_id %></td>
        <td><%= post.query_id %></td>
        <td><%= link_to 'Show', post %></td>
        <td><%= link_to 'Edit', edit_post_path(post) %></td>
        <td><%= link_to 'Destroy', post, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody> -->
</table>

<br>

<!--<%= link_to 'New Post', new_post_path %> -->

<%= form_tag("/posts/search", method: "get") do %>
  <%= label_tag(:keyword, "Keyword:") %>
  <%= text_field_tag(:keyword) %>
  <%= submit_tag("Search") %>
<% end %>

<!-- <form name="search" action="posts/search"
  method="post"> 
  Keyword:<br>
  <input type="text" name="keywords"> 
  <input type="submit" value="Search">
</form>
 -->